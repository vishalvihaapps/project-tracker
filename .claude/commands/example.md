---
description: Example custom slash command — rename this file and edit the prompt below
---

This is a sample project-scoped slash command. Invoke it by typing `/example` in Claude Code.

Use `$ARGUMENTS` to capture everything the user typed after the command name, or
`$1`, `$2`, ... for individual positional arguments.

Replace this body with the instructions you want Claude to follow when the command runs.

Example task using the argument:

> Summarize the file at: $ARGUMENTS
